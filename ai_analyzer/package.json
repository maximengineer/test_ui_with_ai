{
  "name": "ai-analyzer-service",
  "version": "1.0.0",
  "description": "Node.js service that proxies UI regression analysis requests to Google Gemini. Validates incoming requests against the JSON Schema generated from Pydantic contracts in test_ui/contracts/.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --check server.js && node --check scripts/validate.js && echo 'syntax OK (Vitest contract tests deferred - see plan A.4)'"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "@google/genai": "^1.51.0",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "express": "^5.2.1"
  }
}
